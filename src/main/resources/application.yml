spring:
  application:
    name: JOOQ

  datasource:
    url: jdbc:mysql://localhost:3306/sakila
    username: sakila
    password: sakila
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org:
      jooq:
        tools:
          LoggerListener: DEBUG
